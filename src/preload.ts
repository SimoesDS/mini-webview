window.addEventListener("DOMContentLoaded", () => {
  setTimeout(() => {
    console.clear()
    console.log('window.loadURL = _urlId => location.href = `https://www.youtube-nocookie.com/embed/${_urlId}?rel=0&autoplay=1`')
  },
  1500);      
});